{
  "name": "webapp-experiments",
  "version": "1.0.0",
  "description": "WebView app for Android and iOS using Capacitor",
  "scripts": {
    "build": "echo 'Static site, no build step needed'",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios",
    "serve": "npx serve www"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^6.0.2"
  }
}
